<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>A Voz do Povo · Quinari em Foco</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;700;800&family=DM+Mono:ital,wght@0,400;0,500&family=Lora:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
<style>
:root {
  --g:  #00c87a; --g2: #00ff9d; --g3: rgba(0,200,122,.1);
  --am: #f5c518; --verm: #ff5f5f; --az: #4da6ff; --roxo: #a78bfa;
  --f0: #040809; --f1: #080f0c; --f2: #0d1812; --f3: #121f18; --f4: #192a1f;
  --txt: #e0ede6; --mut: #527a62; --brd: #162419; --r: 10px;
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
html{scroll-behavior:smooth;}
body{background:var(--f0);color:var(--txt);font-family:'Lora',Georgia,serif;font-size:15px;line-height:1.65;overflow-x:hidden;}
body::before{content:'';position:fixed;inset:0;background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");pointer-events:none;z-index:0;opacity:.4;}

@keyframes fadeUp{from{opacity:0;transform:translateY(18px)}to{opacity:1;transform:translateY(0)}}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.3}}
@keyframes shimmer{0%{background-position:-200% 0}100%{background-position:200% 0}}
.f1{animation:fadeUp .6s ease both}
.f2{animation:fadeUp .6s .1s ease both}
.f3{animation:fadeUp .6s .2s ease both}

/* NAV */
nav{position:sticky;top:0;z-index:200;background:rgba(4,8,9,.9);backdrop-filter:blur(16px);border-bottom:1px solid var(--brd);padding:0 28px;display:flex;align-items:center;justify-content:space-between;height:56px;gap:16px;}
.nav-logo{font-family:'Syne',sans-serif;font-weight:800;font-size:17px;color:#fff;text-decoration:none;display:flex;align-items:center;gap:9px;}
.nav-logo-dot{width:7px;height:7px;border-radius:50%;background:var(--g);animation:pulse 2s infinite;}
.nav-logo span{color:var(--g);}
.nav-back{font-family:'DM Mono',monospace;font-size:11px;color:var(--mut);text-decoration:none;padding:6px 12px;border:1px solid var(--brd);border-radius:6px;transition:color .2s,border-color .2s;}
.nav-back:hover{color:var(--g);border-color:var(--g);}
.nav-right{display:flex;align-items:center;gap:10px;}
.btn-login{font-family:'DM Mono',monospace;font-size:11px;color:var(--txt);background:transparent;border:1px solid var(--brd);padding:7px 14px;border-radius:6px;cursor:pointer;transition:all .2s;}
.btn-login:hover{border-color:var(--g);color:var(--g);}
.btn-cad{font-family:'DM Mono',monospace;font-size:11px;background:var(--g);color:var(--f0);border:none;padding:7px 14px;border-radius:6px;cursor:pointer;font-weight:500;transition:background .2s;}
.btn-cad:hover{background:var(--g2);}
.user-chip{font-family:'DM Mono',monospace;font-size:11px;color:var(--g);background:rgba(0,200,122,.1);border:1px solid rgba(0,200,122,.25);padding:6px 12px;border-radius:20px;display:flex;align-items:center;gap:7px;}
.user-chip-dot{width:6px;height:6px;border-radius:50%;background:var(--g);}

/* HERO */
.hero{position:relative;padding:64px 40px 56px;background:var(--f1);border-bottom:1px solid var(--brd);overflow:hidden;}
.hero-bg{position:absolute;inset:0;background:radial-gradient(ellipse 70% 80% at 90% 40%,rgba(167,139,250,.06) 0%,transparent 60%),radial-gradient(ellipse 50% 60% at -10% 70%,rgba(0,200,122,.05) 0%,transparent 60%);pointer-events:none;}
.hero-grid{position:absolute;inset:0;background-image:linear-gradient(rgba(0,200,122,.03) 1px,transparent 1px),linear-gradient(90deg,rgba(0,200,122,.03) 1px,transparent 1px);background-size:48px 48px;pointer-events:none;}
.hero-inner{position:relative;max-width:820px;}
.hero-pre{font-family:'DM Mono',monospace;font-size:10px;color:var(--roxo);letter-spacing:3px;text-transform:uppercase;margin-bottom:18px;display:flex;align-items:center;gap:10px;}
.hero-pre::before{content:'';width:24px;height:1px;background:var(--roxo);}
h1{font-family:'Syne',sans-serif;font-weight:800;font-size:clamp(30px,5.5vw,62px);line-height:1.0;color:#fff;letter-spacing:-1.5px;margin-bottom:12px;}
h1 .acc{color:var(--roxo);}
.hero-sub{font-family:'Lora',serif;font-style:italic;font-size:clamp(15px,2vw,19px);color:var(--mut);max-width:580px;line-height:1.6;margin-bottom:32px;}
.hero-tabs{display:flex;gap:6px;flex-wrap:wrap;}
.htab{font-family:'DM Mono',monospace;font-size:12px;padding:10px 20px;border-radius:8px;cursor:pointer;border:1px solid var(--brd);color:var(--mut);background:var(--f2);transition:all .2s;text-decoration:none;}
.htab.active{background:var(--roxo);border-color:var(--roxo);color:#fff;}
.htab:hover:not(.active){border-color:var(--roxo);color:var(--roxo);}

/* AVISO */
.aviso{background:rgba(167,139,250,.06);border:1px solid rgba(167,139,250,.2);border-left:3px solid var(--roxo);border-radius:8px;padding:14px 18px;margin:28px 40px 0;font-family:'DM Mono',monospace;font-size:11px;color:var(--mut);line-height:1.7;}
.aviso strong{color:var(--txt);}

/* MAIN */
main{max-width:1100px;margin:0 auto;padding:48px 28px 80px;}

/* SECTION TABS */
.sec-tabs{display:flex;gap:4px;margin-bottom:36px;background:var(--f2);border:1px solid var(--brd);border-radius:12px;padding:5px;}
.stab{font-family:'DM Mono',monospace;font-size:12px;padding:10px 22px;border-radius:8px;cursor:pointer;color:var(--mut);border:none;background:transparent;transition:all .2s;flex:1;text-align:center;}
.stab.on{background:var(--f3);color:#fff;box-shadow:0 2px 8px rgba(0,0,0,.3);}

/* PANEL */
.panel{display:none;}
.panel.on{display:block;}

/* SECTION LABEL */
.slabel{font-family:'DM Mono',monospace;font-size:10px;color:var(--roxo);letter-spacing:3px;text-transform:uppercase;margin-bottom:20px;display:flex;align-items:center;gap:12px;}
.slabel::after{content:'';flex:1;height:1px;background:var(--brd);}

/* PROJETOS CÂMARA */
.proj-grid{display:flex;flex-direction:column;gap:14px;margin-bottom:40px;}

.proj-card{background:var(--f2);border:1px solid var(--brd);border-radius:14px;overflow:hidden;transition:border-color .2s;}
.proj-card:hover{border-color:rgba(167,139,250,.3);}
.proj-card-header{padding:20px 22px 16px;border-bottom:1px solid var(--brd);}
.proj-top{display:flex;align-items:flex-start;gap:14px;margin-bottom:12px;}
.proj-num{font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);background:var(--f3);border:1px solid var(--brd);padding:3px 8px;border-radius:4px;white-space:nowrap;flex-shrink:0;margin-top:2px;}
.proj-titulo{font-family:'Syne',sans-serif;font-size:16px;font-weight:700;color:#fff;line-height:1.25;}
.proj-meta{display:flex;gap:10px;flex-wrap:wrap;align-items:center;}
.proj-autor{font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);}
.proj-status{font-family:'DM Mono',monospace;font-size:10px;padding:3px 10px;border-radius:20px;}
.st-ap{background:rgba(0,200,122,.1);color:var(--g);border:1px solid rgba(0,200,122,.25);}
.st-rj{background:rgba(255,95,95,.1);color:var(--verm);border:1px solid rgba(255,95,95,.25);}
.st-tr{background:rgba(245,197,24,.1);color:var(--am);border:1px solid rgba(245,197,24,.25);}
.proj-desc{font-size:13.5px;color:var(--mut);line-height:1.65;margin-top:10px;}

.proj-votacao{padding:16px 22px;background:var(--f3);display:flex;align-items:center;gap:20px;flex-wrap:wrap;}
.pvoto-label{font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);text-transform:uppercase;letter-spacing:1px;white-space:nowrap;}
.pvoto-barra-wrap{flex:1;min-width:160px;}
.pvoto-barra{height:8px;border-radius:8px;background:var(--f4);overflow:hidden;display:flex;margin-bottom:5px;}
.pv-ap{background:linear-gradient(to right,var(--g),var(--g2));}
.pv-rj{background:linear-gradient(to right,#ff5f5f,#ff3a3a);}
.pvoto-nums{display:flex;justify-content:space-between;font-family:'DM Mono',monospace;font-size:10px;}
.pv-ng{color:var(--g);}
.pv-nr{color:var(--verm);}
.pvoto-btns{display:flex;gap:8px;flex-shrink:0;}
.pbtn{font-family:'DM Mono',monospace;font-size:11px;padding:8px 16px;border-radius:8px;cursor:pointer;border:1px solid var(--brd);background:var(--f2);color:var(--mut);transition:all .2s;display:flex;align-items:center;gap:6px;}
.pbtn.pbtn-ap:hover,.pbtn.pbtn-ap.voted{background:rgba(0,200,122,.12);border-color:var(--g);color:var(--g);}
.pbtn.pbtn-rj:hover,.pbtn.pbtn-rj.voted{background:rgba(255,95,95,.12);border-color:var(--verm);color:var(--verm);}
.pbtn:disabled{opacity:.4;cursor:not-allowed;}

/* IDEIAS POPULARES */
.nova-ideia-btn{background:var(--roxo);border:none;color:#fff;font-family:'DM Mono',monospace;font-size:12px;font-weight:500;padding:12px 22px;border-radius:10px;cursor:pointer;display:inline-flex;align-items:center;gap:8px;margin-bottom:28px;transition:background .2s;}
.nova-ideia-btn:hover{background:#9171f0;}

/* MODAL */
.modal-overlay{position:fixed;inset:0;background:rgba(0,0,0,.75);z-index:500;display:flex;align-items:center;justify-content:center;padding:20px;backdrop-filter:blur(4px);}
.modal-overlay.hidden{display:none;}
.modal{background:var(--f2);border:1px solid var(--brd);border-radius:16px;width:100%;max-width:520px;overflow:hidden;animation:fadeUp .3s ease;}
.modal-header{padding:20px 24px;border-bottom:1px solid var(--brd);display:flex;align-items:center;justify-content:space-between;}
.modal-title{font-family:'Syne',sans-serif;font-size:17px;font-weight:700;color:#fff;}
.modal-close{background:none;border:none;color:var(--mut);font-size:20px;cursor:pointer;padding:4px;line-height:1;transition:color .2s;}
.modal-close:hover{color:var(--verm);}
.modal-body{padding:24px;}
.modal-footer{padding:16px 24px;border-top:1px solid var(--brd);display:flex;justify-content:flex-end;gap:10px;}

/* FORM */
.form-group{margin-bottom:18px;}
label{font-family:'DM Mono',monospace;font-size:11px;color:var(--mut);display:block;margin-bottom:7px;text-transform:uppercase;letter-spacing:.8px;}
input[type=text],input[type=email],input[type=password],textarea,select{width:100%;background:var(--f3);border:1px solid var(--brd);border-radius:8px;padding:12px 14px;color:var(--txt);font-family:'Lora',serif;font-size:14px;outline:none;transition:border-color .2s;resize:vertical;}
input::placeholder,textarea::placeholder{color:var(--mut);}
input:focus,textarea:focus{border-color:var(--roxo);}
.char-count{font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);text-align:right;margin-top:4px;}
.form-hint{font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);margin-top:5px;line-height:1.5;}
.form-error{font-family:'DM Mono',monospace;font-size:11px;color:var(--verm);margin-top:6px;display:none;}
.form-error.show{display:block;}
.form-success{background:rgba(0,200,122,.08);border:1px solid rgba(0,200,122,.2);border-radius:8px;padding:12px 16px;font-family:'DM Mono',monospace;font-size:12px;color:var(--g);margin-bottom:16px;display:none;}
.form-success.show{display:block;}

.btn-pr{background:var(--roxo);border:none;color:#fff;font-family:'DM Mono',monospace;font-size:12px;font-weight:500;padding:11px 22px;border-radius:8px;cursor:pointer;transition:background .2s;}
.btn-pr:hover{background:#9171f0;}
.btn-cancel{background:none;border:1px solid var(--brd);color:var(--mut);font-family:'DM Mono',monospace;font-size:12px;padding:11px 18px;border-radius:8px;cursor:pointer;transition:all .2s;}
.btn-cancel:hover{border-color:var(--mut);}
.btn-g{background:var(--g);border:none;color:var(--f0);font-family:'DM Mono',monospace;font-size:12px;font-weight:500;padding:11px 22px;border-radius:8px;cursor:pointer;transition:background .2s;}
.btn-g:hover{background:var(--g2);}

.tab-link{color:var(--roxo);cursor:pointer;font-family:'DM Mono',monospace;font-size:12px;text-decoration:underline;background:none;border:none;padding:0;display:inline;}
.tab-link:hover{color:#9171f0;}

/* IDEIAS GRID */
.ideias-grid{display:flex;flex-direction:column;gap:14px;}

.ideia-card{background:var(--f2);border:1px solid var(--brd);border-radius:14px;overflow:hidden;transition:border-color .2s;}
.ideia-card:hover{border-color:rgba(167,139,250,.3);}
.ideia-header{padding:20px 22px 14px;}
.ideia-top{display:flex;align-items:flex-start;gap:12px;margin-bottom:10px;}
.ideia-cat{font-family:'DM Mono',monospace;font-size:9px;padding:3px 9px;border-radius:20px;border:1px solid var(--brd);color:var(--mut);background:var(--f3);white-space:nowrap;flex-shrink:0;margin-top:3px;}
.ideia-titulo{font-family:'Syne',sans-serif;font-size:15px;font-weight:700;color:#fff;line-height:1.3;}
.ideia-desc{font-size:13px;color:var(--mut);line-height:1.65;margin-bottom:12px;}
.ideia-autor{font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);display:flex;align-items:center;gap:6px;}
.ideia-autor::before{content:'✍';font-size:11px;}
.ideia-footer{padding:14px 22px;background:var(--f3);border-top:1px solid var(--brd);display:flex;align-items:center;justify-content:space-between;gap:16px;flex-wrap:wrap;}
.assinaturas-info{display:flex;align-items:center;gap:10px;}
.assinaturas-num{font-family:'Syne',sans-serif;font-size:22px;font-weight:800;color:var(--roxo);}
.assinaturas-label{font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);text-transform:uppercase;letter-spacing:.8px;line-height:1.4;}
.assinar-btn{font-family:'DM Mono',monospace;font-size:11px;font-weight:500;padding:10px 20px;border-radius:8px;cursor:pointer;border:none;transition:all .2s;display:flex;align-items:center;gap:7px;}
.assinar-btn.can{background:var(--roxo);color:#fff;}
.assinar-btn.can:hover{background:#9171f0;}
.assinar-btn.done{background:rgba(0,200,122,.1);border:1px solid rgba(0,200,122,.25);color:var(--g);cursor:default;}
.assinar-btn.login-req{background:var(--f2);border:1px solid var(--brd);color:var(--mut);}
.assinar-btn.login-req:hover{border-color:var(--roxo);color:var(--roxo);}

/* LISTA ASSINATURAS */
.assinaturas-lista{margin:0 22px 16px;background:var(--f3);border:1px solid var(--brd);border-radius:10px;overflow:hidden;max-height:180px;overflow-y:auto;}
.ass-item{padding:10px 16px;border-bottom:1px solid var(--brd);display:flex;align-items:center;gap:10px;font-size:13px;}
.ass-item:last-child{border-bottom:none;}
.ass-check{color:var(--g);font-size:12px;flex-shrink:0;}
.ass-nome{color:var(--txt);font-weight:600;}
.ass-data{font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);margin-left:auto;}
.ver-assinaturas{font-family:'DM Mono',monospace;font-size:11px;color:var(--roxo);cursor:pointer;background:none;border:none;padding:0;margin:0 22px 14px;display:block;}
.ver-assinaturas:hover{color:#9171f0;}

/* LOADING */
.skeleton{background:linear-gradient(90deg,var(--f2) 25%,var(--f3) 50%,var(--f2) 75%);background-size:200% 100%;animation:shimmer 1.5s infinite;border-radius:6px;}

/* EMPTY STATE */
.empty{text-align:center;padding:60px 20px;color:var(--mut);}
.empty-icon{font-size:48px;margin-bottom:16px;opacity:.5;}
.empty-txt{font-size:14px;line-height:1.7;}

/* FILTROS */
.filtros{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:24px;}
.filtro-btn{font-family:'DM Mono',monospace;font-size:10px;padding:6px 14px;border-radius:20px;border:1px solid var(--brd);background:var(--f2);color:var(--mut);cursor:pointer;transition:all .2s;}
.filtro-btn.on{border-color:var(--roxo);color:var(--roxo);background:rgba(167,139,250,.08);}
.filtro-btn:hover:not(.on){border-color:var(--mut);}

/* FOOTER */
footer{background:var(--f1);border-top:1px solid var(--brd);text-align:center;padding:24px;font-family:'DM Mono',monospace;font-size:11px;color:var(--mut);}
footer a{color:var(--g);text-decoration:none;}

@media(max-width:700px){
  .hero{padding:40px 20px 44px;}
  main{padding:32px 16px 60px;}
  .aviso{margin:20px 16px 0;}
  .proj-votacao{flex-direction:column;align-items:flex-start;}
  .pvoto-barra-wrap{width:100%;}
}
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="index.html" class="nav-logo">
    <div class="nav-logo-dot"></div>
    Quinari <span>em Foco</span>
  </a>
  <a href="index.html" class="nav-back">← Voltar ao portal</a>
  <div class="nav-right" id="nav-right">
    <button class="btn-login" onclick="abrirModal('login')">Entrar</button>
    <button class="btn-cad" onclick="abrirModal('cadastro')">Cadastrar</button>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-grid"></div>
  <div class="hero-inner">
    <p class="hero-pre f1">Participação Popular · Senador Guiomard</p>
    <h1 class="f2">"A voz do povo<br><span class="acc">é a voz de Deus."</span></h1>
    <p class="hero-sub f3">Opine sobre os projetos votados na Câmara. Proponha ideias para o município. Assine abaixo-assinados digitais. Porque a democracia não termina no dia da eleição.</p>
    <div class="hero-tabs f3">
      <a class="htab active" onclick="mostrarPainel('camara',this)">🏛️ Projetos da Câmara</a>
      <a class="htab" onclick="mostrarPainel('ideias',this)">💡 Ideias da Comunidade</a>
    </div>
  </div>
</section>

<div class="aviso">
  <strong>Importante:</strong> Os projetos da Câmara são dados públicos e não representam opinião do portal. As votações populares são métricas de opinião da comunidade, sem caráter oficial. As ideias e abaixo-assinados são de autoria dos cidadãos cadastrados. O Quinari em Foco não se responsabiliza pelo conteúdo publicado pelos usuários.
</div>

<!-- CONTEÚDO -->
<main>

  <!-- TAB: CÂMARA -->
  <div class="panel on" id="panel-camara">

    <div class="filtros">
      <button class="filtro-btn on" onclick="filtrarProj('todos',this)">Todos</button>
      <button class="filtro-btn" onclick="filtrarProj('aprovado',this)">✅ Aprovados</button>
      <button class="filtro-btn" onclick="filtrarProj('rejeitado',this)">❌ Rejeitados</button>
      <button class="filtro-btn" onclick="filtrarProj('tramitando',this)">⏳ Em tramitação</button>
    </div>

    <div class="slabel">O que a câmara votou — e o que você acha</div>

    <div class="proj-grid" id="proj-lista">
      <!-- Gerado por JS -->
    </div>

  </div>

  <!-- TAB: IDEIAS -->
  <div class="panel" id="panel-ideias">

    <div style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:12px;margin-bottom:24px;">
      <div class="slabel" style="margin-bottom:0;flex:1;">Ideias e abaixo-assinados criados pela comunidade</div>
      <button class="nova-ideia-btn" onclick="novaIdeia()" style="margin-bottom:0;">✏️ Propor nova ideia</button>
    </div>

    <div class="filtros">
      <button class="filtro-btn on" onclick="filtrarIdeia('todos',this)">Todas</button>
      <button class="filtro-btn" onclick="filtrarIdeia('infraestrutura',this)">🛣️ Infraestrutura</button>
      <button class="filtro-btn" onclick="filtrarIdeia('saude',this)">🏥 Saúde</button>
      <button class="filtro-btn" onclick="filtrarIdeia('educacao',this)">📚 Educação</button>
      <button class="filtro-btn" onclick="filtrarIdeia('esporte',this)">⚽ Esporte e Lazer</button>
      <button class="filtro-btn" onclick="filtrarIdeia('outros',this)">➕ Outros</button>
    </div>

    <div class="ideias-grid" id="ideias-lista">
      <!-- Gerado por JS -->
    </div>

  </div>

</main>

<!-- FOOTER -->
<footer>
  <a href="index.html">← Quinari em Foco</a> · Portal independente · Senador Guiomard, Acre · 2026 ·
  <a href="mailto:quinari.ouvidoria@gmail.com">quinari.ouvidoria@gmail.com</a>
</footer>

<!-- ══════════════════════════════════════
     MODAIS
══════════════════════════════════════ -->

<!-- MODAL LOGIN -->
<div class="modal-overlay hidden" id="modal-login">
  <div class="modal">
    <div class="modal-header">
      <span class="modal-title">🔑 Entrar no portal</span>
      <button class="modal-close" onclick="fecharModal('login')">✕</button>
    </div>
    <div class="modal-body">
      <div class="form-success" id="login-success">Login realizado com sucesso!</div>
      <div class="form-error" id="login-erro">E-mail ou senha incorretos.</div>
      <div class="form-group">
        <label>E-mail</label>
        <input type="email" id="login-email" placeholder="seu@email.com">
      </div>
      <div class="form-group">
        <label>Senha</label>
        <input type="password" id="login-senha" placeholder="••••••••">
      </div>
      <p style="font-family:'DM Mono',monospace;font-size:11px;color:var(--mut);">
        Não tem conta? <button class="tab-link" onclick="fecharModal('login');abrirModal('cadastro')">Cadastre-se grátis</button>
      </p>
    </div>
    <div class="modal-footer">
      <button class="btn-cancel" onclick="fecharModal('login')">Cancelar</button>
      <button class="btn-g" onclick="fazerLogin()">Entrar →</button>
    </div>
  </div>
</div>

<!-- MODAL CADASTRO -->
<div class="modal-overlay hidden" id="modal-cadastro">
  <div class="modal">
    <div class="modal-header">
      <span class="modal-title">📋 Criar conta</span>
      <button class="modal-close" onclick="fecharModal('cadastro')">✕</button>
    </div>
    <div class="modal-body">
      <div class="form-success" id="cad-success">Conta criada! Agora você pode assinar abaixo-assinados.</div>
      <div class="form-error" id="cad-erro">Esse e-mail já está cadastrado.</div>
      <div class="form-group">
        <label>Nome completo</label>
        <input type="text" id="cad-nome" placeholder="Seu nome completo">
        <div class="form-hint">Será exibido nos abaixo-assinados que você assinar.</div>
      </div>
      <div class="form-group">
        <label>E-mail</label>
        <input type="email" id="cad-email" placeholder="seu@email.com">
      </div>
      <div class="form-group">
        <label>Senha</label>
        <input type="password" id="cad-senha" placeholder="Mínimo 6 caracteres">
      </div>
      <p style="font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);line-height:1.6;">
        Ao criar conta, você concorda que seu nome será exibido publicamente nos abaixo-assinados que assinar. Não publicamos sua senha ou e-mail.
      </p>
    </div>
    <div class="modal-footer">
      <button class="btn-cancel" onclick="fecharModal('cadastro')">Cancelar</button>
      <button class="btn-pr" onclick="fazerCadastro()">Criar conta →</button>
    </div>
  </div>
</div>

<!-- MODAL NOVA IDEIA -->
<div class="modal-overlay hidden" id="modal-ideia">
  <div class="modal" style="max-width:580px;">
    <div class="modal-header">
      <span class="modal-title">💡 Propor nova ideia</span>
      <button class="modal-close" onclick="fecharModal('ideia')">✕</button>
    </div>
    <div class="modal-body">
      <div class="form-success" id="ideia-success">Sua ideia foi publicada! A comunidade já pode assinar.</div>
      <div class="form-group">
        <label>Título da ideia</label>
        <input type="text" id="ideia-titulo" placeholder="Ex: Construção de quadra poliesportiva no Ramal São Francisco" maxlength="100" oninput="atualizarChar(this,'char-titulo',100)">
        <div class="char-count"><span id="char-titulo">0</span>/100</div>
      </div>
      <div class="form-group">
        <label>Categoria</label>
        <select id="ideia-cat">
          <option value="infraestrutura">🛣️ Infraestrutura e Ramais</option>
          <option value="saude">🏥 Saúde</option>
          <option value="educacao">📚 Educação</option>
          <option value="esporte">⚽ Esporte e Lazer</option>
          <option value="outros">➕ Outros</option>
        </select>
      </div>
      <div class="form-group">
        <label>Descrição detalhada</label>
        <textarea id="ideia-desc" rows="5" placeholder="Descreva a sua ideia com detalhes: onde, por que é importante, quem seria beneficiado..." maxlength="600" oninput="atualizarChar(this,'char-desc',600)"></textarea>
        <div class="char-count"><span id="char-desc">0</span>/600</div>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn-cancel" onclick="fecharModal('ideia')">Cancelar</button>
      <button class="btn-pr" onclick="publicarIdeia()">Publicar ideia →</button>
    </div>
  </div>
</div>

<!-- MODAL ASSINAR -->
<div class="modal-overlay hidden" id="modal-assinar">
  <div class="modal">
    <div class="modal-header">
      <span class="modal-title">✍️ Assinar abaixo-assinado</span>
      <button class="modal-close" onclick="fecharModal('assinar')">✕</button>
    </div>
    <div class="modal-body">
      <div id="assinar-info" style="background:var(--f3);border:1px solid var(--brd);border-radius:8px;padding:14px 16px;margin-bottom:18px;">
        <div style="font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);margin-bottom:4px;">Você está assinando:</div>
        <div id="assinar-titulo-preview" style="font-family:'Syne',sans-serif;font-size:14px;font-weight:700;color:#fff;line-height:1.3;"></div>
      </div>
      <div class="form-group">
        <label>Confirme seu nome completo</label>
        <input type="text" id="assinar-nome" placeholder="Nome completo para o abaixo-assinado">
        <div class="form-hint">Seu nome será exibido publicamente na lista de assinaturas.</div>
      </div>
      <p style="font-family:'DM Mono',monospace;font-size:10px;color:var(--mut);line-height:1.6;margin-top:4px;">
        Ao assinar, você confirma que é cidadão de Senador Guiomard ou que apoia esta causa como cidadão do Brasil.
      </p>
    </div>
    <div class="modal-footer">
      <button class="btn-cancel" onclick="fecharModal('assinar')">Cancelar</button>
      <button class="btn-pr" onclick="confirmarAssinatura()">✅ Assinar →</button>
    </div>
  </div>
</div>

<script>
// ══════════════════════════════════════
// ESTADO
// ══════════════════════════════════════
const DB_KEY = 'qf_db';

function getDB() {
  try { return JSON.parse(localStorage.getItem(DB_KEY)) || initDB(); }
  catch { return initDB(); }
}

function saveDB(db) {
  localStorage.setItem(DB_KEY, JSON.stringify(db));
}

function initDB() {
  const db = {
    users: [],
    sessao: null,
    projetos: [
      {
        id:'p1', num:'PL 001/2025',
        titulo:'Criação do Programa Municipal de Hortas Comunitárias',
        autor:'Ver. Exemplo A', partido:'PT', data:'12/03/2025',
        status:'aprovado',
        desc:'Institui o programa de criação e manutenção de hortas comunitárias nos bairros e ramais do município, com fornecimento de mudas, adubo e assistência técnica pela Secretaria de Agricultura.',
        votosAp:0, votosRj:0, meuvoto:null
      },
      {
        id:'p2', num:'PL 002/2025',
        titulo:'Obrigatoriedade de câmeras de segurança nos órgãos públicos',
        autor:'Ver. Exemplo B', partido:'MDB', data:'18/03/2025',
        status:'tramitando',
        desc:'Determina a instalação obrigatória de câmeras de segurança em todas as repartições públicas municipais, com imagens disponíveis para consulta via portal da transparência.',
        votosAp:0, votosRj:0, meuvoto:null
      },
      {
        id:'p3', num:'PL 003/2025',
        titulo:'Isenção de IPTU para imóveis de pequenos produtores rurais',
        autor:'Ver. Exemplo C', partido:'PROGRESSISTAS', data:'02/04/2025',
        status:'rejeitado',
        desc:'Concede isenção total do Imposto Predial e Territorial Urbano para propriedades com área até 5 hectares cujo proprietário comprove atividade de produção familiar.',
        votosAp:0, votosRj:0, meuvoto:null
      }
    ],
    ideias: [
      {
        id:'i1', cat:'esporte',
        titulo:'Quadra poliesportiva coberta no Ramal São Francisco',
        desc:'O Ramal São Francisco tem mais de 400 famílias e não possui nenhum espaço de lazer para os jovens. Uma quadra coberta permitiria aulas de educação física, campeonatos e eventos culturais para toda a comunidade.',
        autor:'Maria Souza', data:'10/04/2025',
        assinaturas:[{nome:'Maria Souza',data:'10/04/2025'},{nome:'João da Silva',data:'11/04/2025'}]
      },
      {
        id:'i2', cat:'infraestrutura',
        titulo:'Iluminação pública no trecho final da Rua das Palmeiras',
        desc:'O trecho entre o número 340 e o 520 da Rua das Palmeiras está completamente sem iluminação há mais de 2 anos. Moradores têm medo de sair à noite e já houve assaltos no local.',
        autor:'Pedro Lima', data:'15/04/2025',
        assinaturas:[{nome:'Pedro Lima',data:'15/04/2025'},{nome:'Ana Costa',data:'16/04/2025'},{nome:'Carlos Rocha',data:'16/04/2025'}]
      }
    ]
  };
  saveDB(db);
  return db;
}

// ══════════════════════════════════════
// AUTH
// ══════════════════════════════════════
function getSessao() { return getDB().sessao; }

function atualizarNav() {
  const s = getSessao();
  const nr = document.getElementById('nav-right');
  if (s) {
    const firstName = s.nome.split(' ')[0];
    nr.innerHTML = `
      <div class="user-chip"><div class="user-chip-dot"></div>${firstName}</div>
      <button class="btn-login" onclick="sair()">Sair</button>
    `;
  } else {
    nr.innerHTML = `
      <button class="btn-login" onclick="abrirModal('login')">Entrar</button>
      <button class="btn-cad" onclick="abrirModal('cadastro')">Cadastrar</button>
    `;
  }
}

function fazerCadastro() {
  const nome  = document.getElementById('cad-nome').value.trim();
  const email = document.getElementById('cad-email').value.trim().toLowerCase();
  const senha = document.getElementById('cad-senha').value;
  const err   = document.getElementById('cad-erro');
  const ok    = document.getElementById('cad-success');
  err.classList.remove('show'); ok.classList.remove('show');

  if (!nome || !email || senha.length < 6) {
    err.textContent = 'Preencha todos os campos. Senha mínimo 6 caracteres.';
    err.classList.add('show'); return;
  }

  const db = getDB();
  if (db.users.find(u => u.email === email)) {
    err.textContent = 'Esse e-mail já está cadastrado.';
    err.classList.add('show'); return;
  }

  db.users.push({ nome, email, senha });
  db.sessao = { nome, email };
  saveDB(db);
  ok.classList.add('show');
  setTimeout(() => { fecharModal('cadastro'); atualizarNav(); renderProjetos(); renderIdeias(); }, 1200);
}

function fazerLogin() {
  const email = document.getElementById('login-email').value.trim().toLowerCase();
  const senha = document.getElementById('login-senha').value;
  const err   = document.getElementById('login-erro');
  const ok    = document.getElementById('login-success');
  err.classList.remove('show'); ok.classList.remove('show');

  const db = getDB();
  const user = db.users.find(u => u.email === email && u.senha === senha);
  if (!user) { err.classList.add('show'); return; }

  db.sessao = { nome: user.nome, email: user.email };
  saveDB(db);
  ok.classList.add('show');
  setTimeout(() => { fecharModal('login'); atualizarNav(); renderProjetos(); renderIdeias(); }, 1000);
}

function sair() {
  const db = getDB(); db.sessao = null; saveDB(db);
  atualizarNav(); renderProjetos(); renderIdeias();
}

// ══════════════════════════════════════
// MODAIS
// ══════════════════════════════════════
function abrirModal(id) {
  document.getElementById('modal-' + id).classList.remove('hidden');
  document.body.style.overflow = 'hidden';
}

function fecharModal(id) {
  document.getElementById('modal-' + id).classList.add('hidden');
  document.body.style.overflow = '';
  // Limpar
  ['login-erro','login-success','cad-erro','cad-success','ideia-success'].forEach(i => {
    const el = document.getElementById(i);
    if (el) el.classList.remove('show');
  });
}

document.querySelectorAll('.modal-overlay').forEach(o => {
  o.addEventListener('click', e => { if (e.target === o) { const id = o.id.replace('modal-',''); fecharModal(id); } });
});

// ══════════════════════════════════════
// PAINÉIS / TABS
// ══════════════════════════════════════
function mostrarPainel(id, el) {
  document.querySelectorAll('.panel').forEach(p => p.classList.remove('on'));
  document.querySelectorAll('.htab').forEach(t => t.classList.remove('active'));
  document.getElementById('panel-' + id).classList.add('on');
  if (el) el.classList.add('active');
}

// ══════════════════════════════════════
// PROJETOS DA CÂMARA
// ══════════════════════════════════════
let filtroProj = 'todos';

function filtrarProj(f, el) {
  filtroProj = f;
  document.querySelectorAll('#panel-camara .filtro-btn').forEach(b => b.classList.remove('on'));
  if (el) el.classList.add('on');
  renderProjetos();
}

function renderProjetos() {
  const db    = getDB();
  const lista = document.getElementById('proj-lista');
  const user  = db.sessao;

  let projs = db.projetos;
  if (filtroProj !== 'todos') projs = projs.filter(p => p.status === filtroProj);

  if (!projs.length) {
    lista.innerHTML = `<div class="empty"><div class="empty-icon">🗳️</div><div class="empty-txt">Nenhum projeto encontrado com este filtro.</div></div>`;
    return;
  }

  lista.innerHTML = projs.map(p => {
    const total = p.votosAp + p.votosRj;
    const pctAp = total ? Math.round((p.votosAp / total) * 100) : 0;
    const pctRj = total ? 100 - pctAp : 0;
    const stLabel = {aprovado:'✅ Aprovado na Câmara', rejeitado:'❌ Rejeitado na Câmara', tramitando:'⏳ Em tramitação'}[p.status];
    const stClass = {aprovado:'st-ap', rejeitado:'st-rj', tramitando:'st-tr'}[p.status];
    const myVote  = p.meuvoto;
    const jaVotou = !!myVote;

    return `
    <div class="proj-card" id="proj-${p.id}">
      <div class="proj-card-header">
        <div class="proj-top">
          <span class="proj-num">${p.num}</span>
          <div class="proj-titulo">${p.titulo}</div>
        </div>
        <div class="proj-meta">
          <span class="proj-autor">Por ${p.autor} · ${p.partido} · ${p.data}</span>
          <span class="proj-status ${stClass}">${stLabel}</span>
        </div>
        <div class="proj-desc">${p.desc}</div>
      </div>
      <div class="proj-votacao">
        <span class="pvoto-label">Opinião popular:</span>
        <div class="pvoto-barra-wrap">
          <div class="pvoto-barra">
            <div class="pv-ap" style="width:${pctAp}%"></div>
            <div class="pv-rj" style="width:${pctRj}%"></div>
          </div>
          <div class="pvoto-nums">
            <span class="pv-ng">✅ ${p.votosAp} aprovam (${pctAp}%)</span>
            <span class="pv-nr">❌ ${p.votosRj} reprovam (${pctRj}%)</span>
          </div>
        </div>
        <div class="pvoto-btns">
          <button class="pbtn pbtn-ap ${myVote==='ap'?'voted':''}" ${jaVotou?'disabled':''} onclick="votarProj('${p.id}','ap')">
            👍 Aprovo
          </button>
          <button class="pbtn pbtn-rj ${myVote==='rj'?'voted':''}" ${jaVotou?'disabled':''} onclick="votarProj('${p.id}','rj')">
            👎 Reprovar
          </button>
        </div>
      </div>
    </div>`;
  }).join('');
}

function votarProj(id, voto) {
  const db = getDB();
  const p  = db.projetos.find(x => x.id === id);
  if (!p || p.meuvoto) return;
  p.meuvoto = voto;
  if (voto === 'ap') p.votosAp++; else p.votosRj++;
  saveDB(db);
  renderProjetos();
}

// ══════════════════════════════════════
// IDEIAS POPULARES
// ══════════════════════════════════════
let filtroIdeia = 'todos';
let assinarIdeiaId = null;

function filtrarIdeia(f, el) {
  filtroIdeia = f;
  document.querySelectorAll('#panel-ideias .filtro-btn').forEach(b => b.classList.remove('on'));
  if (el) el.classList.add('on');
  renderIdeias();
}

const catLabels = {
  infraestrutura:'🛣️ Infraestrutura',
  saude:'🏥 Saúde',
  educacao:'📚 Educação',
  esporte:'⚽ Esporte e Lazer',
  outros:'➕ Outros'
};

function renderIdeias() {
  const db    = getDB();
  const lista = document.getElementById('ideias-lista');
  const user  = db.sessao;

  let ideas = [...db.ideias].sort((a,b) => b.assinaturas.length - a.assinaturas.length);
  if (filtroIdeia !== 'todos') ideas = ideas.filter(i => i.cat === filtroIdeia);

  if (!ideas.length) {
    lista.innerHTML = `<div class="empty"><div class="empty-icon">💡</div><div class="empty-txt">Nenhuma ideia encontrada.<br>Seja o primeiro a propor algo para o município!</div></div>`;
    return;
  }

  lista.innerHTML = ideas.map(ideia => {
    const jaAssinou = user && ideia.assinaturas.some(a => a.email === user.email);
    const btnClass  = !user ? 'login-req' : jaAssinou ? 'done' : 'can';
    const btnText   = !user
      ? '🔑 Entre para assinar'
      : jaAssinou
        ? '✅ Você assinou'
        : '✍️ Assinar';
    const btnClick  = !user
      ? `onclick="abrirModal('login')"`
      : jaAssinou
        ? ''
        : `onclick="iniciarAssinatura('${ideia.id}')"`;

    const ultimas = ideia.assinaturas.slice(-3).reverse();
    const listaSig = ultimas.map(a =>
      `<div class="ass-item"><span class="ass-check">✓</span><span class="ass-nome">${a.nome}</span><span class="ass-data">${a.data}</span></div>`
    ).join('');
    const maisLabel = ideia.assinaturas.length > 3 ? `<button class="ver-assinaturas">+ ${ideia.assinaturas.length - 3} outras assinaturas</button>` : '';

    return `
    <div class="ideia-card" id="ideia-${ideia.id}">
      <div class="ideia-header">
        <div class="ideia-top">
          <span class="ideia-cat">${catLabels[ideia.cat] || ideia.cat}</span>
          <div class="ideia-titulo">${ideia.titulo}</div>
        </div>
        <div class="ideia-desc">${ideia.desc}</div>
        <div class="ideia-autor">Proposto por ${ideia.autor} · ${ideia.data}</div>
      </div>
      ${ideia.assinaturas.length > 0 ? `
      <div class="assinaturas-lista">${listaSig}</div>
      ${maisLabel}
      ` : ''}
      <div class="ideia-footer">
        <div class="assinaturas-info">
          <div class="assinaturas-num">${ideia.assinaturas.length}</div>
          <div class="assinaturas-label">assinatura${ideia.assinaturas.length !== 1 ? 's' : ''}</div>
        </div>
        <button class="assinar-btn ${btnClass}" ${btnClick} ${jaAssinou ? 'disabled' : ''}>${btnText}</button>
      </div>
    </div>`;
  }).join('');
}

function iniciarAssinatura(ideiaId) {
  const user = getSessao();
  if (!user) { abrirModal('login'); return; }
  assinarIdeiaId = ideiaId;
  const db    = getDB();
  const ideia = db.ideias.find(i => i.id === ideiaId);
  document.getElementById('assinar-titulo-preview').textContent = ideia.titulo;
  document.getElementById('assinar-nome').value = user.nome;
  abrirModal('assinar');
}

function confirmarAssinatura() {
  const user = getSessao();
  if (!user || !assinarIdeiaId) return;
  const nome = document.getElementById('assinar-nome').value.trim();
  if (!nome) return;

  const db    = getDB();
  const ideia = db.ideias.find(i => i.id === assinarIdeiaId);
  if (!ideia) return;

  if (ideia.assinaturas.some(a => a.email === user.email)) {
    fecharModal('assinar'); return;
  }

  const hoje = new Date().toLocaleDateString('pt-BR');
  ideia.assinaturas.push({ nome, email: user.email, data: hoje });
  saveDB(db);
  fecharModal('assinar');
  renderIdeias();
  assinarIdeiaId = null;
}

// ══════════════════════════════════════
// NOVA IDEIA
// ══════════════════════════════════════
function novaIdeia() {
  const user = getSessao();
  if (!user) { abrirModal('login'); return; }
  abrirModal('ideia');
}

function publicarIdeia() {
  const user  = getSessao();
  if (!user) { fecharModal('ideia'); abrirModal('login'); return; }
  const titulo = document.getElementById('ideia-titulo').value.trim();
  const cat    = document.getElementById('ideia-cat').value;
  const desc   = document.getElementById('ideia-desc').value.trim();
  if (!titulo || !desc) return;

  const db = getDB();
  const hoje = new Date().toLocaleDateString('pt-BR');
  const nova = {
    id: 'i' + Date.now(),
    cat, titulo, desc,
    autor: user.nome,
    email: user.email,
    data: hoje,
    assinaturas: [{ nome: user.nome, email: user.email, data: hoje }]
  };
  db.ideias.unshift(nova);
  saveDB(db);

  document.getElementById('ideia-success').classList.add('show');
  document.getElementById('ideia-titulo').value = '';
  document.getElementById('ideia-desc').value   = '';
  setTimeout(() => { fecharModal('ideia'); renderIdeias(); }, 1300);
}

function atualizarChar(el, targetId, max) {
  document.getElementById(targetId).textContent = el.value.length;
}

// ══════════════════════════════════════
// INIT
// ══════════════════════════════════════
document.addEventListener('DOMContentLoaded', () => {
  getDB(); // init se vazio
  atualizarNav();
  renderProjetos();
  renderIdeias();
});
</script>
</body>
</html>
